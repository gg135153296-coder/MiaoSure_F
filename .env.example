# 复制为 .env.local 后按需修改（.env.local 不会被 git 提交）
# 修改后需重启 npm run dev

# ── 方式一：本机开发，走 Vite 代理（默认，可不写）──
# VITE_API_BASE=/api
# 代理目标在 vite.config.js 的 server.proxy 里改，例如：
#   '/api': 'http://localhost:3000'

# ── 方式二：直连后端 IP（手机联调、局域网、后端不在本机时）──
# 填完整地址，末尾要带 /api（与 request.js 里的 path 拼接）
# VITE_API_BASE=http://192.168.1.6:3000/api
